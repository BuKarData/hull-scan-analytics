{
  "name": "hull-scan-analytics",
  "private": true,
  "version": "0.1.0",
  "description": "Platforma do analizy porownawczej skanow 3D (Gaussian Splatting) kadlubow jednostek plywajacych — historia zmian, heatmapy odksztalcen, wykrywanie usterek.",
  "workspaces": [
    "packages/server",
    "packages/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace packages/server & npm run dev --workspace packages/web",
    "dev:server": "npm run dev --workspace packages/server",
    "dev:web": "npm run dev --workspace packages/web",
    "build": "npm run build --workspace packages/server && npm run build --workspace packages/web",
    "start": "npm run start --workspace packages/server",
    "typecheck": "npm run typecheck --workspace packages/server && npm run typecheck --workspace packages/web"
  }
}
